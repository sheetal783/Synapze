{
  "name": "skillflare",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "yarn install && cd backend && yarn install",
    "start": "cd backend && yarn start"
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0"
  }
}
